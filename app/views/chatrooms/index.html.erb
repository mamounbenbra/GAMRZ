<% if @chatroom.present? %>
  <nav class="nav flex-column">
  <% @chatroom.each do |room| %>
    <%= link_to room.name, room_path(room), class: "nav-link room-nav-link" %>
    <% end %>
  </nav>
<% else %>
  <div class="text-muted">
    <p>The are no rooms</p>
  </div>
<% end %>
