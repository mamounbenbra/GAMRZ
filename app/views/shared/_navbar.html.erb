<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to "/", class: "navbar-brand" do %>
    <i class="fas fa-gamepad icon"></i>
    <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="marginss mr-4" id='username' >
          <%= link_to "Home", "/", class: "text-index", id: "link-nav" %>
        </li>
        <li class="marginss mr-4" id='username' >
          <%= link_to "Swipe", users_path , class: "text-index", id: "link-nav", data: {turbolinks: false} %>
        </li>
        <li class="marginss mr-4" id='matches_wrap' style="position:relative;" >

          <%= link_to "Matches", chatrooms_path , class: "text-index", id: "link-nav" %>
          <% if @empty_chatroom %>
            <div id="notif"></div>
          <% end %>


        </li>
        <li class="nav-item dropdown">
          <%= image_tag (current_user.photo.attached? ? (cl_image_path current_user.photo.key, crop: :fill) : 'https://source.unsplash.com/random'), class: "avatar dropdown-toggle m-0", id: "navbarDropdown", data: { toggle: "dropdown", user_id: current_user.id   }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to 'your account', edit_user_registration_path(@user), class: "dropdown-item" %></p>

            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <li class="text-index mb-4 mr-4">
          <%= link_to "Login", new_user_session_path, class: "text-index nsm pl-4 pr-4 pt-2 pb-2" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>

<style>
  #notif {
    height: 10px;
    width: 10px;
    background: red;
    border-radius: 100%;
    position: absolute;
    top: -1px;
    right: -9px;
  }


</style>



